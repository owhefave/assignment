#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: 0 on success, otherwise an error code
 */
int main(void)
{
	char *message = "Hello, Betty!";

	printf("%s\n", message);
	return (0);
}

